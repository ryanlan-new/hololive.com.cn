[Unit]
Description=Daily Microsoft OAuth client secret health check

[Timer]
# 每天 09:00 本地时间跑一次；随机延迟避开整点，避免和其它定时任务撞在一起
OnCalendar=*-*-* 09:00:00
RandomizedDelaySec=15m
# 机器关机错过的窗口，开机后补跑一次
Persistent=true

[Install]
WantedBy=timers.target
